@model IEnumerable<FinalProjectKidsHealthCenter.Models.VaccineTranasactionTable>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ChildTable.Child_FName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeTable.EMP_FName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VaccineTable.DoseRoute)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DueDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ChildTable.Child_FName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeTable.EMP_FName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VaccineTable.DoseRoute)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DueDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VT_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VT_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VT_ID })
        </td>
    </tr>
}

</table>
