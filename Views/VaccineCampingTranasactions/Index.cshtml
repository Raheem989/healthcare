@model IEnumerable<FinalProjectKidsHealthCenter.Models.VaccineCampingTranasaction>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ChildTable.Child_FName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeTable.EMP_FName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VaccineCampingTable.VC_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ChildTable.Child_FName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeTable.EMP_FName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VaccineCampingTable.VC_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CT_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CT_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CT_ID })
        </td>
    </tr>
}

</table>
